name: Hprofile Actions
on: workflow_dispatch
jobs: 
  Testing:
    runs-on: ubuntu-latest
    steps: 
      - name: Code Checkout
        uses: actions/checkout@v4.2.2
      
      - name: Maven test
        run:  mvn test
      
      - name: checkstyle-10.21.1
        run: mvn checksytle:checkstyle

        

